import "../../main.del";
import ".mmain.del";

rule: "[Mutator] :: Berserk : Set Status"
Event.OngoingPlayer
if (m_enabled[4])
{
    inf_ammo = true;
    delay = 0.5;
    bhop_mult = 150;
    bhop_rate = S_BHOP_SPEED_RATE * 1.5;
    bhop_speed += 50;
    SetMoveSpeed(player, bhop_speed);
}