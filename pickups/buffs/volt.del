import "../../main.del";
import ".bmain.del";

globalvar Number S_VLT_TIME;
globalvar Number S_MAX_VLT_SPREAD;
globalvar Number S_MAX_VLT_DIST;
globalvar Number S_MIN_VLT_DIST;

playervar Any[] vlt_effects; // effects
playervar Number vlt_temp; // increments from 0 - 100 while the player is firing 
playervar Vector vlt_facing; // facing direction (controls spread)
playervar Number vlt_dist; // maximum distance of the laser
playervar Boolean overheat; // if vlt_temp hit 100


disabled rule: "---------------- Volt ----------------"{}

rule: "[Buff] :: Volt : Init"
Event.OnPlayerJoin
{
    overheat = false;
    vlt_dist = S_MAX_VLT_DIST;
}

rule: "[Buff] :: Volt : Action - Fire"
Event.OngoingPlayer
if (buff_enabled[9])
if (!overheat)
if (IsPrimaryHeld())
if (IsAlive())
if (!HasStatus(player, Status.PhasedOut))
{
    if (EntityExists(vlt_effects))
        DestroyEffect(vlt_effects);

    Boolean hit = false;

    WaitIfTrue(0.05); // brief hold
    Vector vlt_hit = RayCastHitPosition(EyePosition(), EyePosition() + Facing() * vlt_dist, AllLivingPlayers(), player);
    Wait(tickdur);

    CreateBeamEffect(all, BeamType.BadBeam, weapon_muzzle(EyePosition(), Facing()), vlt_hit, color_arr[9], EffectRev.VisibleToPositionRadiusAndColor);
    vlt_effects[0] = LastCreatedEntity();
    CreateEffect(all, Effect.WinstonTeslaCannonTargetSound, null, player, 100);
    vlt_effects[1] = LastCreatedEntity();
    CreateEffect(all, Effect.OrisaHaltTendrilSound, null, player, 50 + vlt_temp, Reevaluation: EffectRev.VisibleToPositionRadiusAndColor);
    vlt_effects[2] = LastCreatedEntity();
    CreateEffect(hit ? all : null, Effect.WinstonTeslaCannonSound, null, vlt_hit, 100);
    vlt_effects[3] = LastCreatedEntity();

    while (buff_enabled[9] && !overheat && IsPrimaryHeld() && IsAlive() && !HasStatus(player, Status.PhasedOut))
    {
        AddOverTime(vlt_temp, S_VLT_TIME, 0, 100, tickdur);
        SubtractOverTime(vlt_dist, S_VLT_TIME, S_MIN_VLT_DIST, S_MAX_VLT_DIST, tickdur);

        // slow the rate of ammo depletion the longer fire is held
        if (!inf_ammo)
            buff_ammo[9] -= Abs((vlt_temp - 100) * 0.00645);

        vlt_facing = WorldVectorOf(Vector(TangentFromDegrees(RandomReal(-vlt_temp * S_MAX_VLT_SPREAD, vlt_temp * S_MAX_VLT_SPREAD)), 0, 0), player) + 
        DirectionFromAngles(HorizontalFacingAngleOf(), VerticalFacingAngleOf() + RandomReal(-vlt_temp * S_MAX_VLT_SPREAD, vlt_temp * S_MAX_VLT_SPREAD));

        vlt_hit = RayCastHitPosition(EyePosition(), EyePosition() + vlt_facing * vlt_dist, AllLivingPlayers(), player);
        Player vlt_player = RayCastHitPlayer(EyePosition(), EyePosition() + vlt_facing * vlt_dist, AllLivingPlayers(), player);

        if (DistanceBetween(EyePosition(), vlt_hit) < vlt_dist - 1)
        {
            hit = true;
            PlayEffect(all, PlayEffect.BadExplosion, color_arr[9], vlt_hit, 0.5);
        }
        else 
        {
            hit = false;
        }

        Kill(vlt_player, player);
        Wait(tickdur);

        if (vlt_temp >= 100)
        {
            vlt_temp = 100;
            overheat = true;
            PlayEffect(player, PlayEffect.BaptisteBioticLauncherExplosionSound, Color.White, player, 100);
            CreateEffect(player, Effect.TorbjornOverloadingSound, Color.White, player, 30);
            vlt_effects[4] = LastCreatedEntity();
            break;
        }
    }

    Wait(tickdur);
    DestroyEffect(vlt_effects[0]);
    DestroyEffect(vlt_effects[1]);
    DestroyEffect(vlt_effects[2]);
    DestroyEffect(vlt_effects[3]);

    if (overheat) // check if the player ended the loop because of overheating to prevent the player from automatically firing
    {
        buff_ammo[9] = Ceiling(buff_ammo[9]);

        WaitUntil(!IsPrimaryHeld(), 1);

        pri_override = false;

        WaitUntil(vlt_temp == 0, 99999);

        overheat = false;
        pri_override = true;
        DestroyEffect(vlt_effects[4]);
        PlayEffect(player, PlayEffect.BrigitteRepairPackArmorSound, Color.Team1, player, 100);
    }
}

rule: "[Buff] :: Volt : Action - Decrease Temp"
Event.OngoingPlayer
if (vlt_temp > 0)
if (!IsPrimaryHeld() || overheat)
{
    if (overheat)
        WaitIfTrue(0.5);

    while (vlt_temp > 0 && !IsPrimaryHeld() || overheat)
    {
        SubtractOverTime(vlt_temp, S_VLT_TIME, 0, 100, 0.032);
        AddOverTime(vlt_dist, S_VLT_TIME, S_MIN_VLT_DIST, S_MAX_VLT_DIST, 0.032);
        Wait(0.032);

        if (vlt_temp == 0 && buff_enabled[9])
        {
            overheat = false;
            pri_override = true;
            buff_ammo[9] = Ceiling(buff_ammo[9]);
            break;
        }
    }
}