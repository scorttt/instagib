import "../../main.del";
import ".bmain.del";

disabled rule: "---------------- TBD ----------------"{}

rule: "[Buff] :: TBD Fire Action"
Event.OngoingPlayer
if (buff_enabled[8])
if (IsFiringPrimary())
{

}
