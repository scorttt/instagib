import "../../main.del";
import ".bmain.del";


disabled rule: "---------------- Prism ----------------"{}

rule: "[Buff] :: Prism : Fire Action"
Event.OngoingPlayer
if (buff_enabled[3])
if (IsFiringPrimary())
{
    
}